import {styled} from "@mui/material/styles";
import {BottomNavigationAction} from "@mui/material";

export const BottomStyled = styled(BottomNavigationAction)(`
  &:not(.Mui-disabled) {
    color: #0B9453;
  }
`);